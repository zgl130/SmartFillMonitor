<UserControl x:Class="SmartFillMonitor.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SmartFillMonitor.Views"
             xmlns:smartFillMonitor="clr-namespace:SmartFillMonitor"
             xmlns:hc="clr-namespace:HandyControl.Controls;assembly=HandyControl"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="1200">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/SkinDefault.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/Theme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <TabControl>
        <TabItem Header="Logs">
            <ContentControl Content="{x:Static smartFillMonitor:App.LogView}" />
        </TabItem>
        <TabItem Header="LogAnalyze">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Border Grid.Row="0" Background="{StaticResource AppBgDark}" CornerRadius="4" Padding="8" Margin="0,0,0,10">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="从：" VerticalAlignment="Center" Foreground="White" Margin="0,0,5,0"/>
                        <hc:DateTimePicker SelectedDateTime="{Binding StartDate}" Width="180" VerticalAlignment="Center"/>

                        <TextBlock Text="到：" VerticalAlignment="Center" Foreground="White" Margin="10,0,5,0"/>
                        <hc:DateTimePicker SelectedDateTime="{Binding EndDate}" Width="180" VerticalAlignment="Center"/>
                        
                        <TextBlock Text="等级：" VerticalAlignment="Center" Foreground="White" Margin="20,0,5,0"/>
                        <ComboBox SelectedItem="{Binding SelectLevel ,UpdateSourceTrigger=PropertyChanged}" ItemsSource="{Binding LogLevel}" Width="80" VerticalAlignment="Center"/>

                        <TextBlock Text="搜索：" VerticalAlignment="Center" Foreground="White" Margin="20,0,5,0"/>
                        <TextBox Text="{Binding SearchText ,UpdateSourceTrigger=PropertyChanged}" Width="200" VerticalAlignment="Center" ToolTip="输入消息内容或来源内容进行搜索">
                            <TextBox.InputBindings>
                                <KeyBinding Key="Enter" Command="{Binding SearchCommand}"/>
                            </TextBox.InputBindings>
                        </TextBox>
                        <Button Content="查询" Command="{Binding SearchCommand}" Width="80" Margin="10,0,0,0" Background="{StaticResource AppAccentColor}" Foreground="White"/>
                        <Button Content="导出CSV" Command="{Binding ExportCommand}" Width="80" Margin="10,0,0,0" Background="{StaticResource AppBgPanel}" Foreground="White"/>
                    </StackPanel>
                </Border>
                <DataGrid Grid.Row="1" ItemsSource="{Binding Logs}" AutoGenerateColumns="True" IsReadOnly="True" HeadersVisibility="Column" GridLinesVisibility="Horizontal" BorderThickness="0"/>
                <Grid Grid.Row="2" Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Orientation="Horizontal" Visibility="{Binding IsBusy,Converter={StaticResource Boolean2VisibilityConverter}}" >
                        <TextBlock Text="正在查询" Foreground="LimeGreen" VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                        <TextBlock Text="{Binding TotalCount,StringFormat='共{0}条记录'}" VerticalAlignment="Center" Foreground="Gray" Margin="0,0,20,0"/>
                        <Button Content="上一页" Command="{Binding PrevPageCommand}" Width="80" Background="{StaticResource AppBgPanel}" Foreground="White"/>
                        <TextBlock Text="{Binding TotalCount,StringFormat='第{0}页'}" VerticalAlignment="Center" Foreground="White" Margin="0,0,20,0" FontWeight="Bold"/>
                        <Button Content="下一页" Command="{Binding NextPageCommand}" Width="80" Background="{StaticResource AppBgPanel}" Foreground="White"/>


                    </StackPanel>
                </Grid>
            </Grid>
        </TabItem>
    </TabControl>
</UserControl>
